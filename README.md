# Qt + QML with macOS native API (touchbar, menu, keyboard events)

This repository contains QtQuick QML example is spesific Qt with QML for macOS  
  
Example shows how to populate the MacBook Pro touch bar from a Qt-based applicaiton with QML frontend. Also is shows how to customize main application menu and setup keyboard event handler which create native window and then embed Qt-based application content
